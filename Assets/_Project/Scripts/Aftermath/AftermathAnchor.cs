using UnityEngine;

namespace LastWard.Aftermath
{
    /// <summary>Hand-placed marker (~a few per zone). AftermathManager spawns the chosen template
    /// at whichever anchor is nearest the death position, so bodies land somewhere deliberate
    /// rather than wherever the player happened to die.</summary>
    public class AftermathAnchor : MonoBehaviour
    {
    }
}
