# Backlog

Ideas that come up mid-implementation but are out of scope for the current milestone or the prototype entirely. Don't build these now — this list exists so good ideas aren't lost or, worse, don't quietly creep into the current milestone's scope. See `PROTOTYPE_PLAN.md` §17 for the full list of things already deliberately deferred at planning time.

- _(empty — add items here as they come up during implementation)_
