# CLAUDE.md

Read [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) first — it has the stack, folder map, namespace convention, event bus API, ScriptableObject table list, and the networking sync-surface table. That file is enough for almost every coding task in this repo.

Only read [PROTOTYPE_PLAN.md](PROTOTYPE_PLAN.md) or [the_last_ward_game_bible.md](the_last_ward_game_bible.md) if the task specifically needs design rationale or lore — they're intentionally excluded from routine sessions to keep context small.

Current milestone: see "Milestone Status" in `PROJECT_CONTEXT.md`. Work one milestone step at a time; don't jump ahead to later systems even if they seem related.

Unity Editor is not installed on this machine — see [SETUP.md](SETUP.md) before assuming you can run/build the project.
